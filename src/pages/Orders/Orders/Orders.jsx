
import SectionTitle from '../../../components/SectionTitle';

const Orders = () => {
    return (
        <div>
        <SectionTitle heading="Put Your Orders"></SectionTitle>
        </div>
    );
};

export default Orders;